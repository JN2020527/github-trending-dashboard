name: Update GitHub Trending (Disabled)

on:
  workflow_dispatch:  # 只支持手动触发，禁用自动运行

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Note
        run: |
          echo "⚠️ 此 workflow 已禁用自动运行"
          echo "✅ 数据由本地定时任务生成后推送"
          echo "📅 本地任务时间：每天 9:00（北京时间）"
